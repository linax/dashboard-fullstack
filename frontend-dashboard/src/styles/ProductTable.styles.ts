import { makeStyles } from "@mui/styles"

export const useStyles = makeStyles({
  card: {
    marginTop: 16
  },
  cardContent: {
    textAlign: "center",
    display: "flex",
    alignItems: "center",
    justifyContent: "center"
  },
  title: {
    display: "flex",
    alignItems: "center"
  },
  iconSpacing: {
    marginRight: 8
  },
  actionButtons: {
    display: "flex",
    gap: 8
  }
})
